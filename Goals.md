No overarching goals yet, except getting it to run.

(Background: I intend to move the project from `Documents` to `Projects`. All tests pass for the project in `Documents`, but there are errors when running `make tests` on the copy in `Projects`. Because the project in `Projects` was generated by `git clone`, the files in the `fixtures` can have different timestamps, maybe that's the reason for the errors.)